package collectionLearn.useLib;

import org.apache.commons.collections.map.LinkedMap;
import org.apache.commons.collections4.OrderedMap;

import javax.swing.*;

/*
    OrderedMap  - мапа на которой поддерживается порядок вставки в нее
     */
public class OrderedMapEx {
    /*public static void getWordQuantity(String str){
        String[] words = str.toLowerCase().split("[\\s,;.]+");
        OrderedMap<String, Integer> map = new OrderedMap<>();
        for (String singleWord : words){
            map.merge(singleWord,1,Integer::sum);
        }
        for (String key = map.firstKey(); map.nextKey(key)!= null; key = map.nextKey(key)){
            System.out.println(key + " "+ map.get(key));
        }


    }

    public static void main(String[] args) {
        String str = """
                sfasd
                asdas
                asdfad sd sadfr    rrf f se se s
                fsfesf f efe
                sfesfetr
                tht yj 
                yu
                yui
                 ik
                  
                  
                  kuiuiiuikuyyuy y tythtyht h t trrtg er  tg trg rt gtr rt rrt grtgrtg rth tr tet er 
                   eererg  rt rt rtg trhrthrtge rger rt r r ergrgrty5r5 tt e5t  tdgf e5y ryr t   thhrty  5 yfdf
                """;
        getWordQuantity(str);
    }

    */
}
